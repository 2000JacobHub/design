# Atrium AI · 颜色与字体规范

> 唯一来源：`src/styles/tokens.js`。一切样式必须 `import * as t from "../styles/tokens"` 后通过 `t.xxx` 引用，禁止硬编码 hex。

---

## 一、字体

### 1.1 字体家族

| Token | 用途 | 值 |
|---|---|---|
| `t.sans` | 正文 / UI / 标题 | `'Inter', 'Noto Sans SC', -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Helvetica Neue', sans-serif` |
| `t.mono` | 数字、时间、技术标识、tabular nums | `'JetBrains Mono', 'SF Mono', 'Fira Code', monospace` |

CJK fallback 强制：`Noto Sans SC` 始终在 `Inter` 之后兜底，避免中文字符 fallback 到衬线字体。

SVG `<text>` 必须显式设置 `fontFamily={t.sans}` 或 `fontFamily={t.mono}`，否则会 fallback 到浏览器默认衬线字体。

### 1.2 字号阶梯

| Token | 值 | 用途 |
|---|---|---|
| `t.textXs` | `11px` | 微标签、徽章副文 |
| `t.textXsPlus` | `11.5px` | 微标签变体 |
| `t.textSm` | `12px` | 徽章、辅助说明 |
| `t.textSmPlus` | `12.5px` | 紧凑卡片 body |
| `t.textBaseMinus` | `13px` | 紧凑 body |
| `t.textBase` | `13.5px` | 标准 body |
| `t.textBody` | `14px` | UI body / 标签 |
| `t.textBodyPlus` | `14.5px` | 标题级 body |
| `t.textLg` | `15px` | 区块标题、导航 |
| `t.textMetric` | `22px` | 卡片大数字 |

KPI / Agent 摘要等大数字直接用具体 `fontSize`（20–30px），并加 `fontFeatureSettings: '"tnum"'` 与 `letterSpacing: -0.02 ~ -0.025em`。

### 1.3 字重

| Token | 值 | 使用场景 |
|---|---|---|
| `t.weightNormal` | `400` | 正文默认 |
| `t.weightMedium` | `500` | 标签 / Tab / 按钮 |
| `t.weightSemibold` | `600` | 卡片标题 / 区块标题 / 数字 |
| `t.weightBold` | `700` | 极少使用（仅最高优先级标题） |

单组件内最多出现 2 种 weight，层级通过 weight + color 组合表达，禁止靠 ≥3 种 weight 堆砌层级。

### 1.4 letter-spacing 阶梯

| Token | 值 | 配套字号 |
|---|---|---|
| `t.trackDisplay` | `-2.125px` | 64px display |
| `t.trackHero` | `-1.875px` | 54px |
| `t.trackSection` | `-1.5px` | 48px |
| `t.trackSub` | `-0.625px` | 26px |
| `t.trackCard` | `-0.25px` | 22px |
| `t.trackBadge` | `0.125px` | 12px 徽章 |

CJK 标题内联推荐 `letterSpacing: -0.01em ~ -0.025em`（按字号比例）。

---

## 二、颜色

### 2.1 前景文字（warm gray ramp）

| Token | 值 | 用法 |
|---|---|---|
| `t.fgPrimary` | `rgba(0,0,0,0.95)` | 最强标题（罕用） |
| `t.fgTitle` | `rgba(0,0,0,0.95)` | 标题、KPI 数字 |
| `t.fgStrong` | `#31302e` | 加强文本 |
| `t.fgBody` | `rgba(0,0,0,0.9)` | 正文 |
| `t.fgLabel` | `#31302e` | 字段标签 |
| `t.fgSecondary` | `#4a4744` | 次级文本 |
| `t.fgMuted` | `#615d59` | 弱化文本、辅助 |
| `t.fgDim` | `#615d59` | 同 fgMuted |
| `t.fgSoft` | `#7a7571` | 软化文本 |
| `t.fgHint` | `#8a8581` | 提示、说明 |
| `t.fgGhost` | `#a39e98` | 极弱文本 |
| `t.fgFaint` | `#a39e98` | 同 fgGhost |
| `t.fgWhisper` | `#b8b3ad` | 占位符级 |
| `t.fgMist` | `#c9c4be` | 几乎不可见 |
| `t.fgFog` | `#d8d3cd` | 占位、路人色 |
| `t.fgNearInvisible` | `#e7e2dc` | 极淡分隔 |

禁止直接使用 `#000` 或 `#fff` 作为文字色。

### 2.2 背景

| Token | 值 | 用法 |
|---|---|---|
| `t.bgPrimary` | `#ffffff` | 卡片、面板背景 |
| 页面根背景 | `#fbfaf9` | **所有顶层 view 必须使用此色**（首页、看板、任务、工作流、资产库统一） |
| `t.bgSidebar` / `t.bgAlt` | `#f6f5f4` | 侧栏、备用 surface |
| `t.bgTint` | `rgba(0,0,0,0.02)` | 行 hover 微暗 |
| `t.bgHover` | `rgba(0,0,0,0.04)` | 标准 hover 背景 |
| `t.bgActive` / `t.bgTabActive` | `rgba(0,0,0,0.05)` | 选中态 |
| `t.bgBar` | `rgba(0,0,0,0.04)` | 进度条底色 |
| `t.bgBarFill` | `rgba(0,117,222,0.18)` | 进度条填充 |
| `t.bgBarFillNeg` | `rgba(221,91,0,0.18)` | 进度条负向填充 |

所有"卡片背景"色不得深于 `rgba(0,0,0,0.016)` 叠加在 `#ffffff` 之上。

### 2.3 边框

| Token | 值 | 用法 |
|---|---|---|
| `t.borderLight` | `1px solid rgba(0,0,0,0.06)` | 卡片默认边框（最常用） |
| `t.borderWhisper` / `t.borderDefault` / `t.borderPanel` | `1px solid rgba(0,0,0,0.1)` | 强调边框、下拉边框 |
| `t.borderSuggestion` | `1px solid rgba(0,0,0,0.08)` | 中间强度 |
| `t.borderButtonGhost` / `t.borderThinking` | `1px solid rgba(0,0,0,0.1)` | 按钮 ghost |
| `t.borderInput` | `1px solid #dddddd` | 输入框 |
| `t.borderNote` | `2px solid rgba(0,0,0,0.1)` | 笔记类（罕用，仅 2px 整版） |

禁止 `border-left` / `border-right` 大于 `1px` 的彩色侧边强调条。

### 2.4 强调色（accent）

| Token | 值 | 用法 |
|---|---|---|
| `t.accent` / `t.primaryBlue` | `#0075de` | 主强调色（Notion Blue） |
| `t.accentHover` | `#005bab` | hover 加深 |
| `t.accentFocus` | `#097fe8` | focus |
| `t.accentBadgeBg` | `#f2f9ff` | 蓝徽章背景 |
| `t.accentBadgeText` | `#097fe8` | 蓝徽章文字 |

### 2.5 语义色

| Token | 值 | 用法 |
|---|---|---|
| `t.colorPositive` | `#1aae39` | 完成 / 上涨 / 通过（Notion Green） |
| `t.colorTeal` | `#2a9d99` | 信息成功 / 团队视角（Notion Teal） |
| `t.colorPending` | `#dd5b00` | 警告 / 待处理（Notion Orange） |
| `t.colorNegative` / `t.colorAlert` | `#dd5b00` | 异常 / 下降 / 错误 |
| `t.colorAlertBg` | `rgba(221,91,0,0.04)` | 异常背景 tint |
| `t.colorAlertBorder` | `rgba(221,91,0,0.14)` | 异常边框 |
| `t.colorReview` | `#b8860b` | 审批中 / 待审 / 草稿（Notion Yellow / amber） |
| `t.colorReviewBg` | `rgba(184,134,11,0.08)` | 审批背景 tint |
| `t.colorReviewBorder` | `rgba(184,134,11,0.18)` | 审批边框 |

### 2.6 来源标识（artifact provenance）

| Token | 值 | 用法 |
|---|---|---|
| `t.sourceAi` | `#0075de` | AI 生成（复用 accent） |
| `t.sourceAiBg` | `rgba(0,117,222,0.08)` | AI 生成背景 |
| `t.sourceHuman` | `#615d59` | 人工编辑（**warm grey，禁止用 purple**） |
| `t.sourceHumanBg` | `rgba(0,0,0,0.05)` | 人工编辑背景 |

### 2.7 文件类型色（artifact card 装饰）

| Token | 值 | 类型 |
|---|---|---|
| `t.fileXlsx` | `#1aae39` | 表格 |
| `t.filePdf` | `#dd5b00` | PDF |
| `t.filePptx` | `#b8860b` | PPT |
| `t.fileEmail` | `#615d59` | 邮件 / 压缩包 |
| `t.fileImage` | `#0075de` | 图片 |

### 2.8 业务专用色（首页驾驶舱 AI 推荐 5 卡）

| iconType | icon 色 | tint 背景 | 业务 |
|---|---|---|---|
| `report` | `#c4683a` 暖橙 | `#FFE6D5` | 活动复盘 |
| `marketing` | `#2a9d99` Teal | `#E0F5F3` | 营销方案 |
| `ppt` | `#1d6cb5` 蓝 | `#E6F5FF` | 汇报 PPT |
| `tenant` | `#2d8a4a` 绿 | `#E6FFE9` | 租户分析 |
| `predict` | `#b8860b` 金 | `#FFF5E6` | 营收预测 |

### 2.9 看板热力图色阶

| 用途 | 起点（冷） | 终点（热） |
|---|---|---|
| 客流分析热力图 | `#e0eaff` | `#0075de`（即 `t.accent`） |

---

## 三、禁止项（基于 CLAUDE.md Critical Rules）

1. ❌ 实心黑填充按钮（仅底部 send button ≤28px + ComposePanel submit 例外）
2. ❌ Inter / Roboto / Arial 单独使用（必须配 CJK fallback）
3. ❌ 紫色或彩色渐变（含 `linear-gradient` 用于装饰背景；功能性图表渐变需评估）
4. ❌ border-radius > 12px（除 `radiusFeature` 16px 和 `radiusCircle` 50%）
5. ❌ 卡片背景深于 `rgba(0,0,0,0.016)`
6. ❌ 用 background fill 表达 hover / focus（必须用 border-color / color / opacity）
7. ❌ loading spinner（用 shimmer / pulse / typewriter 替代）
8. ❌ modal（用 inline panel / drawer / 浮层替代；意图识别确认浮卡是允许例外）
9. ❌ emoji 作为 UI 元素（图标用 SVG）
10. ❌ 单组件 >2 种字号
11. ❌ `border-left` / `border-right` 大于 1px 作为彩色强调条
12. ❌ gradient text（`background-clip: text` + gradient）

### 例外：ERP Mock 页面 `/erp-mock`

**故意违反**上述全部规则（深色顶栏、紧凑表格、实心蓝/绿按钮、≤4px 圆角、1.3 紧凑行高），用于营造"传统企业 IT 系统"反差感。这是设计意图，仅限该单一页面。
